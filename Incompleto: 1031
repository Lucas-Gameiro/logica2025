#include <stdio.h>

int main() {

    int N,m=1;
    scanf("%d",&n);
    while (n != 0){
        int regioes[N];
        for(int i = 0; i<N;i++){
            regioes[i] = i+1
        }
        while len(regioes) 
        
        scanf("%d",&n);
    }
    return 0;
}
